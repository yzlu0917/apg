# Paper Draft

Compile from this directory with:

```bash
latexmk -pdf main.tex
```

The draft uses a local fallback `neurips_2024.sty` because no system NeurIPS style file was available in this environment.
