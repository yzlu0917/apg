# Data

This directory is reserved for downloaded or generated task data.

Rules:
- Do not place large datasets here until the gate protocol is frozen.
- Record provenance and snapshot information in `results.md` when data is added.
